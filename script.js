// Constants
const RECTCOUNT = 26;

document.addEventListener("load", (e) => {
    var elementsList = [];
    let main = document.getElementById("main");
    const middleCount = parseInt(Math.ceil(RECTCOUNT * 0.5));
    
    for (var i = 0; i < RECTCOUNT; i++) {
        let element = document.createElement("div");
    }
});